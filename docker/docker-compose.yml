services:
  nginx:
    image: nginx:alpine
    container_name: anexys-nginx
    ports:
      - "80:80"
      # Pas de listen 443 dans nginx.conf (TLS terminé en amont ou à ajouter avec certificats dans ./nginx/ssl).
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      - api
      - frontend
      - keycloak
    networks:
      - anexys-network
    restart: unless-stopped

  frontend:
    build:
      context: ../apps/frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-/api/v1}
        VITE_KEYCLOAK_URL: ${VITE_KEYCLOAK_URL:-}
        VITE_KEYCLOAK_REALM: ${VITE_KEYCLOAK_REALM:-}
        VITE_KEYCLOAK_CLIENT_ID: ${VITE_KEYCLOAK_CLIENT_ID:-}
    container_name: anexys-frontend
    depends_on:
      - api
    networks:
      - anexys-network
    restart: unless-stopped

  migrate:
    build:
      context: ..
      dockerfile: docker/Dockerfile.api
      target: migrator
    container_name: anexys-migrate
    environment:
      - DATABASE_URL=postgresql://anexys:${DB_PASSWORD}@postgres:5432/anexys_master
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - anexys-network
    profiles:
      - tools

  seed-demo:
    build:
      context: ..
      dockerfile: docker/Dockerfile.api
      target: builder
    container_name: anexys-seed-demo
    command: sh -c "cd /app/apps/api && npx ts-node scripts/seed-demo-data.ts"
    environment:
      - DATABASE_URL=postgresql://anexys:${DB_PASSWORD}@postgres:5432/anexys_master
      - VAULT_KEY=${VAULT_KEY:-0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef}
      - SEED_FROM_DOCKER=1
      - PRISMA_QUERY_ENGINE_LIBRARY=/app/apps/api/src/generated/master/libquery_engine-linux-musl-openssl-3.0.x.so.node
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - anexys-network
    profiles:
      - tools

  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile.api
    container_name: anexys-api
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - CONNECTORS_PATH=/app/connectors
      - DATABASE_URL=postgresql://anexys:${DB_PASSWORD}@postgres:5432/anexys_master
      - REDIS_URL=redis://redis:6379
      - BENTHOS_HTTP_URL=${BENTHOS_HTTP_URL:-http://benthos:4195}
      - BENTHOS_REDIS_HEARTBEAT_KEY=${BENTHOS_REDIS_HEARTBEAT_KEY:-benthos_heartbeat}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - VAULT_KEY=${VAULT_KEY}
      # Forcer le moteur Prisma OpenSSL 3 sur Alpine
      - PRISMA_QUERY_ENGINE_LIBRARY=/app/apps/api/dist/generated/master/libquery_engine-linux-musl-openssl-3.0.x.so.node
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - SMTP_FROM=${SMTP_FROM}
      - FRONTEND_URL=${FRONTEND_URL:-https://ultimate.edicloud.app}
      - KEYCLOAK_ISSUER=${KEYCLOAK_ISSUER:-}
      - KEYCLOAK_CLIENT_ID=${KEYCLOAK_CLIENT_ID:-}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ../connectors:/app/connectors:ro
      - pipeline_data:/pipeline
    networks:
      - anexys-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/v1/marketplace"]
      interval: 30s
      timeout: 10s
      retries: 3

  worker:
    build:
      context: ..
      dockerfile: docker/Dockerfile.api
    container_name: anexys-worker
    command: ["node", "dist/worker.js"]
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://anexys:${DB_PASSWORD}@postgres:5432/anexys_master
      - REDIS_URL=redis://redis:6379
      - VAULT_KEY=${VAULT_KEY}
      # Forcer le moteur Prisma OpenSSL 3 sur Alpine (détection sinon "linux-musl" → libssl.so.1.1)
      - PRISMA_QUERY_ENGINE_LIBRARY=/app/apps/api/dist/generated/master/libquery_engine-linux-musl-openssl-3.0.x.so.node
    volumes:
      - pipeline_data:/pipeline
    depends_on:
      redis:
        condition: service_healthy
      api:
        condition: service_started
    networks:
      - anexys-network
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    container_name: anexys-postgres
    environment:
      - POSTGRES_USER=anexys
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=anexys_master
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db:/docker-entrypoint-initdb.d:ro
    networks:
      - anexys-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U anexys -d anexys_master"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Keycloak (BDC-99) — https://auth.edicloud.app — realm `anexys` (import JSON)
  keycloak:
    image: docker.io/keycloak/keycloak:26.0
    container_name: anexys-keycloak
    command: ["start", "--import-realm"]
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/keycloak
      KC_DB_USERNAME: anexys
      KC_DB_PASSWORD: ${DB_PASSWORD}
      KC_HOSTNAME: https://auth.edicloud.app
      KC_HOSTNAME_STRICT: "true"
      KC_PROXY_HEADERS: xforwarded
      KC_HTTP_ENABLED: "true"
      KC_HEALTH_ENABLED: "true"
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KEYCLOAK_ADMIN_USERNAME:-admin}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD}
    volumes:
      - ./keycloak/realms:/opt/keycloak/data/import:ro
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - anexys-network
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: anexys-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    networks:
      - anexys-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Pipeline Benthos (heartbeat JSON → liste Redis), HTTP /ping pour supervision.
  benthos:
    image: jeffail/benthos:4.27.0
    container_name: anexys-benthos
    volumes:
      - ./benthos/benthos.yaml:/benthos.yaml:ro
      - pipeline_data:/pipeline
    command: ["-c", "/benthos.yaml"]
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - anexys-network
    restart: unless-stopped
    ports:
      - "4195:4195"

  # FTP de test (profil e2e) — dépôt des exports CSV du moteur.
  ftp-test:
    image: fauria/vsftpd
    container_name: anexys-ftp-test
    profiles:
      - e2e
    environment:
      FTP_USER: anexys
      FTP_PASS: anexys
      PASV_ADDRESS: ftp-test
      PASV_MIN_PORT: 21100
      PASV_MAX_PORT: 21110
    ports:
      - "2121:21"
      - "21100-21110:21100-21110"
    networks:
      - anexys-network
    restart: unless-stopped

networks:
  anexys-network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  pipeline_data:
